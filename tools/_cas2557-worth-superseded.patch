diff --git a/sim/config.js b/sim/config.js
index aab429f..79904be 100644
--- a/sim/config.js
+++ b/sim/config.js
@@ -3280,6 +3280,45 @@ export const ROLE_SURGE = {
   ],
 };
 
+// CAS-2556: REMATE DE PRESA VELOZ (SWIFT_SURGE, EVO#94 DARK) — EJE VELOCIDAD DE MOVIMIENTO BASE del mob TYPE server-auth (la RAPIDEZ INTRÍNSECA de la criatura: una alimaña escurridiza que corretea — murciélago/kamikaze/lobo — vale más que un plúmbeo que se arrastra — golem/brujo backline/invocador estático) + canal FRESCO swiftFind (recompensa de fichas de acoso por rematar a una PRESA ESCURRIDIZA — la que cuesta acorralar porque huye rápido, no al plantón que ya estaba clavado). Camino al 36º flag (serializa tras #93 ROLE_SURGE LIVE&closed).
+//  · PRE-FLIGHT GATE (recomendado = KILL-EFFORT / Nº de golpes para matar al mob) → FALLA (idéntico a #93 CAS-2551): NO existe un contador entero determinista de golpes-por-mob server-auth. El path de daño hero→enemy (hitEnemy sim.js) hace `e.hp-=dmg` y NO incrementa ningún `e.hits`/`e.timesHit`; spawnEnemy NO estampa contador de impactos. Y aún si se añadiera: nº-de-golpes-para-matar = e.maxHp / dañoPorGolpe-del-héroe ⇒ ENTRELAZADO con el DPS/tempo del héroe (frenesí +dmg/stack, combo/cadence#67 modulan el daño por golpe) ⇒ NO es propiedad INTRÍNSECA del mob y NO es ⊥ cadence#67/frenzy/combo ⇒ FALLA el gate de ortogonalidad ⇒ NO se fuerza (regla del issue).
+//  · PRE-FLIGHT de los alternos SANCIONADOS → los 3 del board tienen defecto: (a) XP-WORTH / valor-de-recompensa base FALLA freshness — #72 ESCASEZ ya lee `tpl.xp` como PROXY DE VALOR (scarcityForageEssence = round(scarcityMul(zone)*tpl.xp), config.js:3996/sim.js) ⇒ una banda-por-xp sería MONÓTONA con la esencia de #72 en la misma zona ⇒ NO ⊥#72; gold≈colineal con xp ⇒ misma contaminación. (b) DAÑO-TOTAL-INFLINGIDO por la víctima FALLA como INTRÍNSECO — requiere un acumulador `e.dmgDealt` NUEVO y es DINÁMICO acoplado a la defensa del héroe (bloqueo/esquiva/i-frames ⇒ 0 amenaza a un bruto perfectamente esquivado) ⇒ mismo defecto de acoplamiento hero-tempo que hundió KILL-EFFORT. (c) EDAD-DEL-MOB / tiempo-vivo FALLA determinismo INTRÍNSECO — spawnEnemy NO hornea timestamp/tick de spawn server-auth (`spawnT no horneado`, ya notado en #91/#93) y es acoplado al ORDEN de kill del héroe. ⇒ PIVOTE JUSTIFICADO al eje FRESCO ESTÁTICO-INTRÍNSECO más limpio: VELOCIDAD BASE.
+//  · PRE-FLIGHT del eje VELOCIDAD BASE → PASA (el MÁS limpio disponible): `spd` es un ESCALAR ENTERO ESTÁTICO por template (config.js:290+; 56..158 en 27 tipos no-jefe), server-auth (constante de ETPL, replicada del mismo config, NO wall-clock, NO estado de cliente, NO RNG, NO DPS-del-héroe), y NINGUNA de las 35 flags #59-#93 lo lee como SCORE (`spd` sólo alimenta la CINEMÁTICA de movimiento en updateEnemies/moveEnt + la cadencia de gait de render — jamás recompensa de kill; los 27 `*Weight` seams del arco NO leen spd). MÁS AÚN: las crux de #92 BULK y #93 ROLE citan EXPLÍCITAMENTE "⊥ velocidad" como el eje RESERVADO-e-INTOCADO ("un summoner lento spd60 vs volatile veloz spd152, ambos con rol propio ≠ rapidez") ⇒ #94 consume precisamente ese eje que los previos certificaron fresco. CLAVE ⊥#74/⊥#85/⊥zona/⊥champion: swiftWeight lee la VELOCIDAD BASE INMUTABLE `ETPL[e.type].spd` (la rapidez del TIPO), NO `e.spd` ni `e.tpl.spd` — el afijo 'Veloz'/'Acorazado' (A.spdMul 1.42/0.92, config.js:467/468), la escala de zona (z.spdMul 1.00..1.20, config.js:621+, aplicada en spawn a e.spd) y el frost-slow de CC (e.slowT/e.slow, #85) escalan el CLON/la entidad viva pero JAMÁS la fila base de ETPL ⇒ un magmabrute base spd56 con afijo 'Veloz' (×1.42=79) SIGUE swift0; un bat base spd158 con 'Acorazado' o congelado por CC SIGUE swift2 ⇒ velocidad-base DESACOPLADA de afijo/zona/CC por construcción.
+//  · EJE server-auth ESTÁTICO: swiftWeight(e) = peso por la BANDA DE VELOCIDAD BASE del TIPO. v=swiftOf(e)=ETPL[e.type].spd (base inmutable). v≥hiSpd(120) ⇒ escurridiza (bat/volatile/rat/wolf) ⇒ swift(2); v≥midSpd(90) ⇒ ágil (mudlurker/bandit/revenant) ⇒ brisk(1); v<midSpd (plúmbeo: casters/brutes/chargers/summoners) ⇒ 0. LUT PURA server-auth data-driven por umbral. NO filtra e.dead (el eje ES la víctima propia, como bulkWeight #92/roleWeight #93 — en el TOP de killEnemy el mob ya tiene e.dead=true pero su TIPO/velocidad-base es inmutable ⇒ señal FIEL). El SCORE del kill = swiftWeight(víctima) muestreado en el TOP de killEnemy. La señal VIVA del badge/VM = swiftScore(hero)=MAX swiftWeight sobre los mobs VIVOS en radio (la presa más escurridiza rematable ahora).
+//  · APLICACIÓN — un solo seam: el TAIL de killEnemy (leyendo el snapshot `_swiftPre`=swiftWeight(e) muestreado en el TOP). Al rematar un mob no-neutral de velocidad-base≥midSpd (swiftWeight≥1 vía `_swiftPre`), el héroe cosecha fichas de acoso = swiftForage(hero, tpl, _swiftPre), banca a h.swiftBounty vía grantSwiftBounty (0 RNG). BYTE-NEUTRO OFF: enabled:false ⇒ `_swiftPre`=0 (const inerte) + rama entera CÓDIGO MUERTO ⇒ killEnemy byte-idéntico al HEAD. FRAME-BUDGET: swiftWeight en el kill = O(1) 1×/kill (1 lookup ETPL); swiftScore (badge/VM) = O(n) sobre mobs-en-radio (n acotado <~20) 1×/frame ⇒ trivial dentro de 16ms.
+//  · ⊥35 (CRUX): eje VELOCIDAD DE MOVIMIENTO BASE ESTÁTICA del TIPO de mob (magnitud de rapidez intrínseca), NO rol/función ni tamaño/área/vida/conteo/geometría/estado/alcance/dirección. ⊥ #93 ROLE [FUNCIÓN de IA arch (enabler/disruptor/brawler); velocidad = MAGNITUD de rapidez — wolf rusher(rol0) es swift2 pero un summoner enabler(rol2, la pieza clave más valiosa) es swift0: DIAMÉTRICAMENTE OPUESTOS; volatile disruptor(rol1) swift2 vs revenant disruptor(rol1) swift1 MISMO rol banda distinta; el rol NO rastrea rapidez]. ⊥ #92 BULK [BANDA DE TAMAÑO físico ETPL[type].size; velocidad = rapidez — bat sz14(bulk0) es swift2 pero moose sz26(bulk2) es swift0 OPUESTOS; wolf swift2/bulk1 vs bat swift2/bulk0 mismo swift distinto bulk]. ⊥ #91 ZONE-TIER [dificultad del ÁREA donde muere (TERRENO); velocidad = propiedad de la ENTIDAD, indep. de zona; además z.spdMul escala e.spd VIVO pero swift lee BASE]. ⊥ #90 EMBESTIDA [DIRECCIÓN de movimiento (signo del producto punto m·u, unit-vector, magnitud-independiente, DINÁMICO por-frame); velocidad = MAGNITUD del stat base ESTÁTICO, dirección-independiente — un bat veloz cargando (heading2) o huyendo (heading0) tiene el MISMO swift2; un summoner lento cargando (heading2/swift0) vs wolf veloz cargando (heading2/swift2) MISMO heading distinto swift]. ⊥ #89 INTERRUPT [ESTADO DE ACCIÓN DINÁMICO e.state; velocidad = stat de spawn inmutable]. ⊥ #88 REMATE [DISTANCIA hero↔víctima]. ⊥ #87 MANADA [clustering mob↔mob]. ⊥ #86 SIEGA/fracción-vida [e.hp/e.maxHp DINÁMICO; velocidad = escalar ESTÁTICO del template — bat a plena vida y bat casi-muerto MISMO swift, OPUESTA siega]. ⊥ #85 CC [e.stun/e.slowT estado IMPUESTO que RALENTIZA la entidad viva; velocidad lee ETPL[type].spd BASE ⇒ un bat congelado por frost SIGUE swift2]. ⊥ #84 ESCARAMUZA [CLASE DE ALCANCE e.tpl.ranged; velocidad = CAMPO DISTINTO, score NO rastrea ranged — TODOS los ranged son lentos (swift0) pero swift0 MEZCLA melee (orc spd64) + ranged (mage spd62) ⇒ la banda NO determina alcance: orc melee swift0/#84 0 vs mage ranged swift0/#84 2 OPUESTOS en #84 con MISMO swift; bat melee swift2/#84 0 vs spearman ranged swift0/#84 2]. ⊥ #74 AFIJO [A.spdMul infla el CLON e.spd; swift lee ETPL[type].spd BASE — magmabrute 'Veloz' sigue swift0]. ⊥ #73 APEX [DISTANCIA a jefe/campeón]. ⊥ #78 FURIA [BOOLEANO e.enraged]. ⊥ #72 ESCASEZ [CONTEO crudo]. ⊥ CADENCE #67/FRENZY/COMBO [tempo/DPS del HÉROE]. ⊥ nivel(inexistente)/edad(spawnT no horneado)/sigilo/terreno/clima/tiempo/social/territorial/backstab-facing(ángulo).
+export const SWIFT_SURGE = {
+  enabled: false,            // DARK — EVO#94 (CAS-2556, 36º flag candidato). Flip false→true tras CEO Gate 1/2 byte-verify + DARK QA Gate 2/2 + CEO Gate 2/2. Base master HEAD 7cc64d7 / served d3a276a13dc0 (#93 ROLE_SURGE LIVE). enabled:false ⇒ TODOS los seams (kill grant + badge/VM) son código muerto ⇒ killEnemy/render byte-idénticos al HEAD.
+  channel: "swiftFind",      // canal FRESCO (recompensa de fichas de acoso por rematar a una PRESA ESCURRIDIZA — la que huye rápido y cuesta acorralar) — NINGUNA de las 35 flags #59-#93 lo usa. La familia recompensa-de-forrajeo EXISTENTE () está LLENA ⇒ fichas de acoso es una moneda FRESCA, transitoria, fuera del save + fingerprint. Fuente ÚNICA (seam de kill) ⇒ máximo-único trivial, sub-cap propio. CEO balance knob.
+  radius: 300,               // radio de CONSIDERACIÓN (px) SÓLO del badge/VM: la señal VIVA = MAX swiftWeight sobre los mobs VIVOS en radio (la presa más escurridiza rematable ahora). El GRANT REAL usa la velocidad-base propia de la víctima (sin cap de radio). CEO balance knob.
+  hiSpd: 120,                // umbral de VELOCIDAD BASE (px/s) para la banda escurridiza (swift, peso 2): bat158/volatile152/rat132/wolf128. CEO balance knob.
+  midSpd: 90,                // umbral de VELOCIDAD BASE (px/s) para la banda ágil (brisk, peso 1): mudlurker112/bandit106/revenant104. <midSpd = plúmbeo ⇒ 0 (casters/brutes/chargers/summoners/healers, spd 56..86). CEO balance knob.
+  weights: { swift: 2, brisk: 1 },   // PESO de un remate por la BANDA DE VELOCIDAD BASE del TIPO del mob: ESCURRIDIZA (spd≥hiSpd) pesa 2; ÁGIL (spd≥midSpd) pesa 1; plúmbeo (spd<midSpd) ⇒ 0. CEO balance knobs.
+  swiftBountyCap: 2,         // SUB-CAP DURO PROPIO de las fichas de acoso por kill — acota el bono aunque la tabla se re-tunee. = max(tiers.charge)=2 ⇒ neutral con la tabla actual. CEO balance knob.
+  // TABLA de tiers por PESO DE VELOCIDAD (score∈{0,1,2}). Presa MÁS escurridiza = tier MÁS ALTO. El tier vigente = el más intenso (mayor `min`) cuyo score se satisface. Score < 1 (plúmbeo) ⇒ Tier 0 ⇒ +0. Determinista, LUT pura.
+  tiers: [
+    { min: 1, charge: 1 },   // Tier 1 — score ≥1 (mob rematado ÁGIL: brisk): +1 ficha de acoso por kill. "acorralaste al escurridizo".
+    { min: 2, charge: 2 },   // Tier 2 — score ≥2 (mob rematado ESCURRIDIZO: swift): +2 fichas por kill. "cazaste a la presa más veloz". CEO balance knobs.
+  ],
+};
+
+// CAS-2557: REMATE DE PRESA VALIOSA (WORTH_SURGE, EVO#94 DARK) — EJE VALOR/RECOMPENSA INTRÍNSECO del mob TYPE server-auth (el mérito/valía de la criatura como PRESA: cuánta recompensa vale en la tabla del bestiario — un revenante/demonio codiciado vs una rata/murciélago de a peón) + canal FRESCO worthFind (recompensa de marcas de recompensa por rematar a un mob de ALTO VALOR/VALÍA — el que despacha a la presa codiciada del bestiario, no a la alimaña de bajo mérito). Camino al 36º flag (serializa tras #93 ROLE_SURGE LIVE&closed).
+// PRE-FLIGHT GATE (obligatorio): el eje RECOMENDADO del issue (THREAT / DAÑO ACUMULADO que ESE mob infligió al héroe) FALLA — NO existe un contador entero determinista por-mob del daño-infligido-al-héroe server-auth: damageHero(dmg,ang,infl,src) (sim.js:8738) usa `src` SÓLO para enrutar parry/escudo/espíritu, NUNCA acumula src.dmgDealt/src.threat; los únicos `dmgDealt` del código son locales hero→enemy (probes de guard-break sim.js:14748) NO enemy→hero; spawnEnemy no estampa acumulador. Añadir uno exigiría MUTAR el enemigo en el hot-path de daño cada golpe (nuevo estado replicado, per-tick) ⇒ el issue manda NO forzarlo ⇒ pivote al alterno FRESCO sancionado XP-WORTH. PASA: `xp` es un ESCALAR ENTERO ESTÁTICO por template (constante de ETPL, replicada del mismo config, determinista, NO wall-clock, NO estado de cliente, NO RNG, NO daño real infligido) y NINGUNA de las 35 flags #59-#93 lo lee como SCORE de kill (`.xp` sólo alimenta la CANTIDAD de XP en gainXP/bounty-board/rested — jamás una BANDA/peso de kill). CLAVE ⊥#91/#74/⊥champion: worthOf lee ETPL[e.type].xp BASE, NO e.tpl.xp — applyZoneScale (sim.js:2303 xp:Math.round(b.xp*z.xpMul)), campeón (2391 t.xp*=C.xpMul) y afijo (2409 t.xp=b.xp*A.xpMul) TODOS inflan el CLON e.tpl.xp, JAMÁS la fila base ⇒ valor DESACOPLADO de zona/afijo/campeón por construcción (más fuerte que #92 size, que sólo afijo/campeón tocan; xp lo tocan LOS TRES). DESACOPLE ⊥#92 size: xp es MÉRITO de diseño (recompensa asignada), size es HITBOX físico — SON INDEPENDIENTES en la tabla real: revenant (size20 mole-media 1) xp64⇒worth ALTO 2, moose (size26 mole-grande 2) xp48⇒worth MEDIO 1 ⇒ el mob MÁS PEQUEÑO vale MÁS xp que el MÁS GRANDE ⇒ BANDAS DISJUNTAS (skeleton size20/xp20 worth0 vs healer size19/xp32 worth1: el más pequeño vale más). Es el desacople EXPLÍCITO leído de la tabla ETPL.
+export const WORTH_SURGE = {
+  enabled: false,            // DARK (EVO#94, CAS-2557). Byte-neutral OFF: la rama de seam queda muerta ⇒ killEnemy byte-idéntico al HEAD (_worthPre=0 const inerte). Flip false→true SÓLO tras CEO Gate 1/2 byte-verify + DARK QA Gate 2/2 (2-cli 0-desync) + CEO Gate 2/2. Base master HEAD ebe67bd3bce5 / served d3a276a13dc0 (#93 ROLE_SURGE LIVE, base de este build).
+  channel: "worthFind",      // canal FRESCO (recompensa de marcas de recompensa por rematar a un mob de ALTO VALOR/VALÍA) — NINGUNA de las 35 flags #59-#93 lo usa. La familia recompensa-de-forrajeo EXISTENTE () está LLENA ⇒ marcas de recompensa es una moneda FRESCA, transitoria, fuera del save + fingerprint. Fuente ÚNICA (seam de kill) ⇒ máximo-único trivial, sub-cap propio. CEO balance knob.
+  radius: 300,               // radio de CONSIDERACIÓN (px) SÓLO del badge/VM: la señal VIVA = MAX worthWeight sobre los mobs VIVOS en radio (la presa más valiosa rematable ahora). El GRANT REAL usa el valor propio de la víctima (sin cap de radio). CEO balance knob.
+  weights: { high: 2, mid: 1 },   // PESO de un remate por la BANDA DE VALOR del TIPO del mob: presa de ALTO valor (xp≥hiXp: demon/revenant + jefes golem/dragon) pesa 2; presa de valor MEDIO (xp≥midXp: spearman/bandit/healer/wraith/summoner/mage/orc/charger/moose) pesa 1; presa de bajo mérito (xp<midXp: rat/bat/wolf/volatile/skeleton) ⇒ 0. CEO balance knobs.
+  hiXp: 50,                  // umbral de BANDA para presa de ALTO valor: ETPL[type].xp BASE ≥ 50 (demon58/revenant64 + jefes golem220/dragon300) ⇒ high(2). ESCALAR ENTERO ESTÁTICO puro (mérito del tipo). CEO balance knob.
+  midXp: 30,                 // umbral de BANDA para presa de valor MEDIO: ETPL[type].xp BASE ≥ 30 (spearman30/bandit30/healer32/wraith32/summoner34/mage36/orc40/charger46/moose48) ⇒ mid(1). Entre 0 y midXp (rat8/bat9/wolf12/volatile16/skeleton20) ⇒ 0. CEO balance knob.
+  worthBountyCap: 2,         // SUB-CAP DURO PROPIO de las marcas de recompensa por kill — acota el bono aunque la tabla se re-tunee. = max(tiers.charge)=2 ⇒ neutral con la tabla actual. CEO balance knob.
+  // TABLA de tiers por PESO DE VALOR (score∈{0,1,2}). Presa de MAYOR valía = tier MÁS ALTO. El tier vigente = el más intenso (mayor `min`) cuyo score se satisface. Score < 1 (presa de bajo mérito) ⇒ Tier 0 ⇒ +0. Determinista, LUT pura.
+  tiers: [
+    { min: 1, charge: 1 },   // Tier 1 — score ≥1 (mob rematado de valor MEDIO: lancero/bandido/médico/orco/embestidor/alce): +1 marca de recompensa por kill. "cobrar la presa mediana".
+    { min: 2, charge: 2 },   // Tier 2 — score ≥2 (mob rematado de ALTO valor: demonio/revenante/golem/dragón): +2 marcas por kill. "rematar a la presa codiciada". CEO balance knobs.
+  ],
+};
+
 // CAS-1879: HOGUERA / REST SITE (Bonfire, 13º pilar · capstone que UNIFICA Estus+Mancha de Sangre+checkpoint).
 // Descansar en un sitio seguro (world.fountains) cura a tope, recarga Estus, fija el ancla de respawn y REPUEBLA los
 // no-jefes de la zona (tradeoff Souls: recuperas recursos pero el mundo vuelve). Sólo en seguridad (sin no-jefes en
diff --git a/sim/sim.js b/sim/sim.js
index 4de042f..a606f42 100644
--- a/sim/sim.js
+++ b/sim/sim.js
@@ -14,7 +14,7 @@
 // in buildWorld, so a fixed seed + identical intent stream => identical sim.
 // ===========================================================================
 import { STR } from "../strings.js";
-import { TS, MAP_W, MAP_H, T_WATER, T_CALDERA, CFG, ATK, ETPL, SPELLS, ACTIVE_ABILITIES, ABILITY_MAP, DEFAULT_LOADOUT, ULTIMATES, ULTIMATE_MAP, ULT_CHARGE_PER_DMG, ULT_CHARGE_PER_KILL, ULT_OFFER_N, ABILITY_RANKS, ABILITY_RANK_MAP, ABILITY_UNLOCKS, CLASS_STATS, HUNTS, ZONE_TIER, ABYSS_POWER_REQ, FROST_POWER_REQ, TRIAL_POWER_REQ, STAGE1_GOAL, STATUS, CONSUMABLES, ATKSPD_TOTAL_CAP, AMBUSH, MOB_AFFIX, MOB_AFFIX_IDS, MOB_AFFIX_RATE, MOB_AFFIX_ESSENCE, CHAMPION, CHAMPION_RATE, LEGENDARY, MASTERY, CUSTOMIZE, BOONS, BOON_MAP, BOON_RARITY, BOON_DRAFT_N, SYNERGIES, boonRarityWeight, ZONE_MODIFIERS, ZONE_MOD_MAP, CURSE_DEPTH_BONUS, CONQUEST_ZONES, WORLD_TIER, ARENA, ZONE_EVENTS, SOCKETS, NEW_MOBS, CODEX, TITLES, PACTS, WEAPON_AFFIXES, FRENZY, PARRY, TELEGRAPH, DODGE, ENEMY_ABILITIES, POISE, COMBO, BACKSTAB, STAMINA, LOCK_ON, FLASK, BLOODSTAIN, SHIELD_BLOCK, GUARD_COUNTER, DODGE_COUNTER, RALLY, RIPOSTE, CHARGED_ATTACK, GUARD_BREAK, DEFLECT, LUNGE, SECOND_WIND, BONFIRE, EQUIP_LOAD, TWO_HAND, HYPERARMOR, WEAPON_ARCHETYPES, WEAPON_ARTS, THROWABLES, WEAPON_BUFFS, STATUS_BUILDUP, ZONE5, CALDERA_POWER_REQ, ZONE5_MOD, SIGNATURE_BOSS, SUMMON, BOSS_RUSH, SEEDED_CHALLENGE, ENCOUNTER_VARIANTS, ARENA_HAZARDS, COMBAT_CODEX, COMBAT_CODEX_ENTRIES, JUICE, ONBOARDING, NG_PLUS, DOORS_INTERIORS, SAFEZONE, TEMPLE_RESPAWN, RESTED_XP, RECALL, BOUNTY_BOARD, SANCTUARY_REP, SANCTUARY_REWARDS, WORLD_EVENT, SANCTUARY_EMISSARY, SANCTUARY_OATH, SANCTUARY_LEDGER, ORDER_STANDINGS, ORDER_TERRITORY, ORDER_CONTEST, FELLOWSHIP_BOND, MENTOR_BOND, SOUL_RECOVERY, WORLD_PULSE, CONGREGATION, WAYFARER_TRAIL, DIVERSE_COMPANY, LONG_WATCH, FRONTIER_SPREAD, INFLUX_SURGE, BATTLE_SYNC, CONVOY_MARCH, WARDING_RING, KINSHIP_BOND, WAYFARER_ROAM, FOCUS_FIRE, TRAILCRAFT, DELVE, ERUDITION, NOCTURNE_HUNT, CADENCE_RUSH, TEMPEST_SURGE, LAST_STAND, FIRM_FOOTING, SHADOW_STALK, SCARCITY_EDGE, APEX_PROXIMITY, MOB_AFFIX_DANGER, ZONE_EVENT_SURGE, ENCOUNTER_VARIANT_SURGE, ARENA_HAZARD_SURGE, BOSS_ENRAGE_SURGE, SPOILS_FIELD_SURGE, CARNAGE_FIELD_SURGE, CROSSFIRE_FRAY_SURGE, MAELSTROM_FIELD_SURGE, BLIGHT_HARVEST_SURGE, SKIRMISH_LINE_SURGE, CONTROL_HARVEST_SURGE, BLOODHARVEST_SURGE, PACKHARVEST_SURGE, LONGSHOT_SURGE, INTERRUPT_SURGE, HEADING_SURGE, ZONETIER_SURGE, BULK_SURGE, ROLE_SURGE, T_GRASS, T_DIRT, T_STONE, T_COBBLE, T_STREET } from "./config.js";
+import { TS, MAP_W, MAP_H, T_WATER, T_CALDERA, CFG, ATK, ETPL, SPELLS, ACTIVE_ABILITIES, ABILITY_MAP, DEFAULT_LOADOUT, ULTIMATES, ULTIMATE_MAP, ULT_CHARGE_PER_DMG, ULT_CHARGE_PER_KILL, ULT_OFFER_N, ABILITY_RANKS, ABILITY_RANK_MAP, ABILITY_UNLOCKS, CLASS_STATS, HUNTS, ZONE_TIER, ABYSS_POWER_REQ, FROST_POWER_REQ, TRIAL_POWER_REQ, STAGE1_GOAL, STATUS, CONSUMABLES, ATKSPD_TOTAL_CAP, AMBUSH, MOB_AFFIX, MOB_AFFIX_IDS, MOB_AFFIX_RATE, MOB_AFFIX_ESSENCE, CHAMPION, CHAMPION_RATE, LEGENDARY, MASTERY, CUSTOMIZE, BOONS, BOON_MAP, BOON_RARITY, BOON_DRAFT_N, SYNERGIES, boonRarityWeight, ZONE_MODIFIERS, ZONE_MOD_MAP, CURSE_DEPTH_BONUS, CONQUEST_ZONES, WORLD_TIER, ARENA, ZONE_EVENTS, SOCKETS, NEW_MOBS, CODEX, TITLES, PACTS, WEAPON_AFFIXES, FRENZY, PARRY, TELEGRAPH, DODGE, ENEMY_ABILITIES, POISE, COMBO, BACKSTAB, STAMINA, LOCK_ON, FLASK, BLOODSTAIN, SHIELD_BLOCK, GUARD_COUNTER, DODGE_COUNTER, RALLY, RIPOSTE, CHARGED_ATTACK, GUARD_BREAK, DEFLECT, LUNGE, SECOND_WIND, BONFIRE, EQUIP_LOAD, TWO_HAND, HYPERARMOR, WEAPON_ARCHETYPES, WEAPON_ARTS, THROWABLES, WEAPON_BUFFS, STATUS_BUILDUP, ZONE5, CALDERA_POWER_REQ, ZONE5_MOD, SIGNATURE_BOSS, SUMMON, BOSS_RUSH, SEEDED_CHALLENGE, ENCOUNTER_VARIANTS, ARENA_HAZARDS, COMBAT_CODEX, COMBAT_CODEX_ENTRIES, JUICE, ONBOARDING, NG_PLUS, DOORS_INTERIORS, SAFEZONE, TEMPLE_RESPAWN, RESTED_XP, RECALL, BOUNTY_BOARD, SANCTUARY_REP, SANCTUARY_REWARDS, WORLD_EVENT, SANCTUARY_EMISSARY, SANCTUARY_OATH, SANCTUARY_LEDGER, ORDER_STANDINGS, ORDER_TERRITORY, ORDER_CONTEST, FELLOWSHIP_BOND, MENTOR_BOND, SOUL_RECOVERY, WORLD_PULSE, CONGREGATION, WAYFARER_TRAIL, DIVERSE_COMPANY, LONG_WATCH, FRONTIER_SPREAD, INFLUX_SURGE, BATTLE_SYNC, CONVOY_MARCH, WARDING_RING, KINSHIP_BOND, WAYFARER_ROAM, FOCUS_FIRE, TRAILCRAFT, DELVE, ERUDITION, NOCTURNE_HUNT, CADENCE_RUSH, TEMPEST_SURGE, LAST_STAND, FIRM_FOOTING, SHADOW_STALK, SCARCITY_EDGE, APEX_PROXIMITY, MOB_AFFIX_DANGER, ZONE_EVENT_SURGE, ENCOUNTER_VARIANT_SURGE, ARENA_HAZARD_SURGE, BOSS_ENRAGE_SURGE, SPOILS_FIELD_SURGE, CARNAGE_FIELD_SURGE, CROSSFIRE_FRAY_SURGE, MAELSTROM_FIELD_SURGE, BLIGHT_HARVEST_SURGE, SKIRMISH_LINE_SURGE, CONTROL_HARVEST_SURGE, BLOODHARVEST_SURGE, PACKHARVEST_SURGE, LONGSHOT_SURGE, INTERRUPT_SURGE, HEADING_SURGE, ZONETIER_SURGE, BULK_SURGE, ROLE_SURGE, WORTH_SURGE, T_GRASS, T_DIRT, T_STONE, T_COBBLE, T_STREET } from "./config.js";
 import { clamp, lerp, dist2, norm, angDiff } from "./math.js";
 import { createRNG } from "./rng.js";
 import { buildWorld, buildTiledWorld, zoneOf } from "./world.js";
@@ -4667,6 +4667,41 @@ export function roleVM(h){ h=h||G.hero; const on=!!ROLE_SURGE.enabled&&!!h;
     cap:Math.max(0,ROLE_SURGE.roleBountyCap|0), radius:+ROLE_SURGE.radius||0,
     tag: roleTag(h) }; }
 
+// CAS-2557: REMATE DE PRESA VALIOSA (WORTH_SURGE) — EJE VALOR/RECOMPENSA INTRÍNSECO del mob TYPE server-auth (el mérito/valía de la criatura como PRESA en la tabla del bestiario) + canal FRESCO worthFind (marcas de recompensa por rematar a un mob de ALTO VALOR/VALÍA — la presa codiciada, no la alimaña de bajo mérito). Funciones PURAS, deterministas, 0-RNG/0-timer/STATELESS, server-auth. El valor = ETPL[e.type].xp (escalar ENTERO BASE inmutable del template, ⊥ zona/afijo/campeón que inflan el CLON e.tpl.xp: applyZoneScale sim.js:2303, campeón 2391, afijo 2409). VALOR INTRÍNSECO ESTÁTICO del TIPO: cuánta recompensa VALE la criatura, NO tamaño (⊥#92), NO área (⊥#91), NO rol (⊥#93), NO vida (⊥#86), NO alcance (⊥#84), NO distancia (⊥#88/#73), NO estado (⊥#89/#85), NO daño-infligido (⊥ THREAT recomendado, sin contador). PRE-FLIGHT GATE: el eje recomendado (THREAT / daño acumulado por-mob al héroe) FALLA — NO existe contador entero determinista por-mob del daño-infligido (damageHero sim.js:8738 usa `src` sólo para enrutar parry/escudo/espíritu, jamás acumula src.dmgDealt) ⇒ pivote al alterno FRESCO sancionado XP-WORTH. PASA: `xp` es escalar entero determinista por template y NINGUNA de las 35 flags #59-#93 lo lee como SCORE de kill.
+// worthOf(e) = VALOR BASE del TIPO del mob (xp del ETPL, inmutable). Lee ETPL[e.type].xp (⊥ zona/afijo/campeón que mutan el CLON e.tpl.xp, NO la fila base). Fallback e.tpl.xp si el tipo faltara (probes). 0 si no hay xp. PURA, server-auth.
+function worthOf(e){ if(!e) return 0; const base=ETPL[e.type]; if(base && base.xp!=null) return +base.xp||0; return (e.tpl&&e.tpl.xp!=null)?(+e.tpl.xp||0):0; }
+// worthWeight(e) = peso de UN mob por la BANDA de su VALOR/RECOMPENSA. xp=worthOf(e). xp≥hiXp(50) ⇒ presa ALTO valor ⇒ high(2); xp≥midXp(30) ⇒ presa valor MEDIO ⇒ mid(1); xp<midXp (presa de bajo mérito) ⇒ 0. LUT PURA server-auth ESTÁTICA. NO filtra e.dead (el eje ES la víctima propia, como bulkWeight #92/roleWeight #93 — en el TOP de killEnemy el mob ya tiene e.dead=true pero su TIPO/valor BASE es inmutable ⇒ señal FIEL).
+function worthWeight(e){ if(!e) return 0; const cfg=WORTH_SURGE, W=cfg.weights||{};
+  const xp=worthOf(e);                                                            // valor/recompensa BASE del tipo del mob (ETPL[type].xp, ⊥ clon zona/afijo/campeón)
+  const hi=(cfg.hiXp!=null)?+cfg.hiXp:50, mid=(cfg.midXp!=null)?+cfg.midXp:30;
+  if(xp>=hi) return +W.high||0;                                                   // presa de alto valor ⇒ 2
+  if(xp>=mid) return +W.mid||0;                                                   // presa de valor medio ⇒ 1
+  return 0; }                                                                     // presa de bajo mérito ⇒ 0
+// worthScore(h) = MEJOR presa DISPONIBLE ahora = MAX worthWeight sobre los mobs VIVOS cuyo centro cae dentro de WORTH_SURGE.radius del héroe (la presa más valiosa rematable). PURO/determinista ⇒ MISMO valor para todo observador del mismo snapshot replicado (G.enemies + tipos). 0 si no hay presa de valor≥medio cerca. Señal VIVA del badge/VM (el GRANT REAL usa el valor PROPIO de la víctima, sin cap de radio). O(n) sobre mobs (n acotado cerca del héroe).
+function worthScore(h){ h=h||G.hero; if(!h) return 0; const cfg=WORTH_SURGE, R=+cfg.radius||0, R2=R*R; let best=0;
+  for(const e of (G.enemies||[])){ if(!e||e.dead||e.hp<=0) continue; const dx=h.x-e.x, dy=h.y-e.y, d2=dx*dx+dy*dy; if(d2>R2) continue; const w=worthWeight(e); if(w>best) best=w; } return best; }
+// worthRank(score) = índice del tier de valor vigente (0 = presa de bajo mérito) = el MÁS INTENSO (mayor `min`) cuyo score se satisface. Presa de MAYOR valía = tier ALTO. LUT determinista pura.
+function worthRank(score){ const T=WORTH_SURGE.tiers||[]; let t=0; for(let i=0;i<T.length;i++){ if(score>=(+T[i].min||0)) t=i+1; } return t; }
+// worthBonus(score) = nº de marcas de recompensa del tier vigente, acotado por el sub-cap propio worthBountyCap. Gated ⇒ OFF ⇒ 0 EXACTO (byte-neutral). PURO. Seguridad de canal: sólo alimenta worthFind.
+function worthBonus(score){ if(!WORTH_SURGE.enabled) return 0;
+  if((WORTH_SURGE.channel||"worthFind")!=="worthFind") return 0;                   // seguridad: WORTH_SURGE SÓLO alimenta worthFind (si el knob se re-apunta, no contribuye)
+  const t=worthRank(score); if(t<=0) return 0;
+  const raw=+WORTH_SURGE.tiers[t-1].charge||0, cap=Math.max(0,WORTH_SURGE.worthBountyCap|0);
+  return (cap>0?Math.min(cap,raw):raw)|0; }
+// worthForage(h, tpl, preScore) = las marcas por un remate a un mob de valor≥medio = worthBonus(preScore). preScore = worthWeight(víctima) muestreado en el TOP de killEnemy con el tipo VIVO del mob (antes del splice) ⇒ señal FIEL al instante del kill. Si preScore es undefined (VM/preview) usa worthScore(h) EN VIVO. 0 si OFF / presa de bajo mérito / sin tpl. PURO — el seam decide banca a h.worthBounty vía grantWorthBounty.
+function worthForage(h, tpl, preScore){ if(!WORTH_SURGE.enabled||!tpl) return 0;
+  const score=(preScore==null)?worthScore(h||G.hero):(+preScore||0); return worthBonus(score); }
+// worthTag(h) = glifo del badge de REMATE DE PRESA VALIOSA (✦) si hay una presa de valor≥medio en radio (tier>0). PURO. "" si OFF / sin presa valiosa cerca.
+export function worthTag(h){ h=h||G.hero; if(!WORTH_SURGE.enabled||!h) return "";
+  return worthRank(worthScore(h))>0 ? "✦" : ""; }
+// worthVM(h) = view-model PURO para el badge/HUD (autoridad en sim ⇒ MISMO score/tier/marcas para todos los clientes del mismo estado). 0 sim/RNG/side-effect.
+export function worthVM(h){ h=h||G.hero; const on=!!WORTH_SURGE.enabled&&!!h;
+  const score=on?worthScore(h):0, tier=on?worthRank(score):0, charge=on?worthBonus(score):0;
+  return { enabled:!!WORTH_SURGE.enabled, channel:WORTH_SURGE.channel||"worthFind",
+    score, tier, tierCount:(WORTH_SURGE.tiers||[]).length, charge,
+    cap:Math.max(0,WORTH_SURGE.worthBountyCap|0), radius:+WORTH_SURGE.radius||0,
+    tag: worthTag(h) }; }
+
 // CAS-2361: CAMARADERÍA / KINSHIP BOND (DARK, KINSHIP_BOND) — EVO mecánica #60. Canal FRESCO goldFind (bono de oro, ⊥ restedMult/wardRegen) + eje FRESCO PERSISTENCIA DE VÍNCULO (proximidad
 // pareada SOSTENIDA). server-authoritative: el server toma las posiciones de los presentes, las asigna a celdas coarse (cellSize) y cuenta los PARES (i<j) cuyas celdas distan Chebyshev≤1
 // (próximos); mientras ≥minPairs pares se sostienen ACUMULA un `kinship` con DECAY, empuja { zona → { kinship, atMs } }; el cliente REFLEJA + PROYECTA al `now` compartido. kinshipPairs(positions)
@@ -6036,6 +6071,8 @@ function killEnemy(e){
   const _bulkPre = BULK_SURGE.enabled ? bulkWeight(e) : 0;
   // CAS-2551 ROLE_SURGE: snapshot del PESO DE ROL = roleWeight(e) muestreado AQUÍ, en el TOP, con el TIPO VIVO del mob `e` (ETPL[e.type].arch — la muerte fija e.dead=true pero NO altera el TIPO ni su rol BASE) ⇒ señal FIEL al rol del instante del kill. NO es densidad de vecindad (⊥ auto-conteo N/A: el eje ES la víctima propia, como #92 mole/#88 remate/#91 zona) — es el ROL DE COMBATE de ESTE mob. GATED ⇒ enabled:false ⇒ `_rolePre`=0 (const inerte, 0 side-effect) ⇒ byte-idéntico al HEAD.
   const _rolePre = ROLE_SURGE.enabled ? roleWeight(e) : 0;
+  // CAS-2557 WORTH_SURGE: snapshot del PESO DE VALOR = worthWeight(e) muestreado AQUÍ, en el TOP, con el TIPO VIVO del mob `e` (ETPL[e.type].xp BASE — la muerte fija e.dead=true pero NO altera el TIPO ni su valor BASE; y worthOf lee la fila base, NO el clon e.tpl.xp inflado por zona/afijo/campeón) ⇒ señal FIEL al valor del instante del kill. NO es densidad de vecindad (⊥ auto-conteo N/A: el eje ES la víctima propia, como #93 rol/#92 mole/#88 remate/#91 zona) — es el VALOR/RECOMPENSA INTRÍNSECO de ESTE mob. GATED ⇒ enabled:false ⇒ `_worthPre`=0 (const inerte, 0 side-effect) ⇒ byte-idéntico al HEAD.
+  const _worthPre = WORTH_SURGE.enabled ? worthWeight(e) : 0;
   // CAS-1773: MEDIDOR DE FRENESÍ — a real (non-neutral) kill adds a stack if within the window,
   // else re-arms at 1 (first kill after a full decay). Pure arithmetic, 0 RNG, gated on FRENZY.enabled.
   if(FRENZY.enabled && G.hero && !tpl.neutral && !G.hero.dead){
@@ -6303,6 +6340,12 @@ function killEnemy(e){
   if(ROLE_SURGE.enabled && !tpl.neutral){ const rv=roleForage(G.hero, tpl, _rolePre);
     if(rv>0){ grantRoleBounty(rv);
       floater(e.x,e.y-320,"+"+rv+" Cabecilla","#b892e0",{small:true}); } }
+  // CAS-2557 WORTH_SURGE seam: REMATE DE PRESA VALIOSA. Al matar un mob no-neutral de valor≥medio (worthWeight≥1 vía `_worthPre`, muestreado en el TOP con el TIPO VIVO de `e`: presa de ALTO valor [demon/revenant + jefes golem/dragon, ETPL[type].xp≥hiXp] ⇒ peso 2, presa de valor MEDIO [spearman/bandit/healer/orc/charger/moose, xp≥midXp] ⇒ 1, presa de bajo mérito [rat/bat/wolf/volatile/skeleton, xp<midXp] ⇒ 0), el
+  // héroe cosecha marcas de recompensa = worthForage(hero,tpl,_worthPre) (flat por banda de valor, sub-cap worthBountyCap), banca a h.worthBounty vía grantWorthBounty (0 RNG). El eje ES el VALOR/RECOMPENSA INTRÍNSECO de ESTA víctima ⇒ rematar a la alimaña de bajo mérito NO forrajea; hace falta despachar a la presa codiciada del bestiario.
+  // Canal FRESCO worthFind (fuente ÚNICA, sub-cap worthBountyCap) — NINGUNA de las 35 flags #59-#93 lo toca; marcas transitorias ⇒ fuera del save + fingerprint. GATED ⇒ enabled:false ⇒ rama muerta: 0 marcas, 0 floater, 0 grantWorthBounty ⇒ killEnemy byte-idéntico al HEAD.
+  if(WORTH_SURGE.enabled && !tpl.neutral){ const wv=worthForage(G.hero, tpl, _worthPre);
+    if(wv>0){ grantWorthBounty(wv);
+      floater(e.x,e.y-336,"+"+wv+" Recompensa","#f0d878",{small:true}); } }
   G.enemies.splice(G.enemies.indexOf(e),1);
 }
 
@@ -7544,6 +7587,8 @@ function grantTierBounty(n){ const h=G.hero; if(!h||n<=0) return; h.tierBounty=(
 function grantBulkBounty(n){ const h=G.hero; if(!h||n<=0) return; h.bulkBounty=(h.bulkBounty|0)+(n|0); }
 // CAS-2551: banca fichas de cabecilla (canal roleFind de ROLE_SURGE). Moneda TRANSITORIA NUEVA (h.roleBounty — init LAZY por-run, FUERA del save allowlist + fingerprint). 0 RNG. Mirror de grantBulkBounty para el trickle de cabecilla al rematar a un mob de alto valor táctico (habilitador/disruptor). STATELESS: h.roleBounty NO se serializa ⇒ no es clave de save.
 function grantRoleBounty(n){ const h=G.hero; if(!h||n<=0) return; h.roleBounty=(h.roleBounty|0)+(n|0); }
+// CAS-2557: banca marcas de recompensa (canal worthFind de WORTH_SURGE). Moneda TRANSITORIA NUEVA (h.worthBounty — init LAZY por-run, FUERA del save allowlist + fingerprint). 0 RNG. Mirror de grantRoleBounty para el trickle de recompensa al rematar a una presa de alto valor/valía (ETPL[type].xp). STATELESS: h.worthBounty NO se serializa ⇒ no es clave de save.
+function grantWorthBounty(n){ const h=G.hero; if(!h||n<=0) return; h.worthBounty=(h.worthBounty|0)+(n|0); }
 
 // CAS-1889: CARGA DE EQUIPO — helper DERIVADO puro. Suma el peso de las 3 piezas equipadas (slotWeight·rarityWeight)
 // y lo divide por la capacidad ⇒ ratio ⇒ banda (fast/mid/fat/over). Aritmética 100% sobre {slot,rarity} ya en save.v1
@@ -11295,6 +11340,55 @@ export const dev = {
       precedence:"roleFind (canal FRESCO — recompensa de fichas de cabecilla por REMATAR a un mob de ALTO VALOR TÁCTICO — habilitador/pieza clave del pack): NINGUNA de las 34 flags #59-#92 lo toca. La familia recompensa-de-forrajeo de moneda EXISTENTE (goldFind, lootQuality, xpGain, essenceFind, matFind, flaskPotency, gemFind, socketFind, healPotency, trophyFind #78, salvageFind #79, boneFind #80, frayFind #81, maelstromFind #82, blightFind #83, skirmishFind #84, controlFind #85, bloodFind #86, packFind #87, reachFind #88, interruptFind #89, headingFind #90, tierFind #91, bulkFind #92) está LLENA ⇒ pivota a una moneda FRESCA, fichas de cabecilla (h.roleBounty, recurso TRANSITORIO NUEVO, fuera del save allowlist + worldFingerprint). Fuente ÚNICA (seam de kill) ⇒ máximo-único trivial, sub-cap propio roleBountyCap, 0 doble-dip. PRE-FLIGHT GATE: el eje RECOMENDADO del issue (KILL-EFFORT / Nº de golpes para matar al mob) FALLA — NO existe un contador entero determinista de golpes-por-mob server-auth: hitEnemy (sim.js:5668) hace e.hp-=dmg (sim.js:5966) SIN incrementar e.hits/e.timesHit/nº-de-instancias-de-daño, y spawnEnemy (sim.js:2258) NO estampa contador de impactos; y aún si se añadiera, nº-de-golpes-para-matar = e.maxHp / dañoPorGolpe-del-héroe ⇒ ENTRELAZADO con el DPS/tempo del héroe (frenesí +dmg/stack, combo/cadence#67) ⇒ NO propiedad INTRÍNSECA del mob y NO ⊥ cadence#67/frenzy/combo ⇒ pivote justificado al alterno FRESCO sancionado ARQUETIPO/ROL. PASA: `arch` es un CATEGÓRICO ESTÁTICO por template (constante de ETPL, replicada del mismo config, NO wall-clock, NO estado de cliente, NO RNG, NO DPS-del-héroe) y NINGUNA de las 34 flags previas lo lee como SCORE (`arch` sólo IA de movimiento [sim.js:4519/8225/8255/8269] + gate de pool de afijos [excluye volatile] + telegraph de render). CLAVE ⊥#73/⊥champion: roleWeight lee ETPL[e.type].arch (ROL BASE INMUTABLE del TIPO), NO e.tpl.arch — la promoción a campeón (sim.js:6318) LIMPIA el CLON e.tpl.arch=undefined pero NUNCA la fila base de ETPL ⇒ rol DESACOPLADO de campeón/afijo por construcción. EJE ESTÁTICO = roleWeight(e)=banda de roleBand(roleOf(e)): enabler(summoner/healer, dmg:0) ⇒ 2, disruptor(warlock/volatile/punisher) ⇒ 1, brawler(brute/charger/rusher/caster) ⇒ 0. score del kill = roleWeight(víctima) muestreado en TOP (_rolePre); señal VIVA del badge = roleScore(hero)=MAX roleWeight sobre mobs VIVOS en radio. CRUX ⊥34: eje ROL/FUNCIÓN DE COMBATE ESTÁTICO del TIPO, NO tamaño/área/vida/conteo/geometría/estado/alcance. ⊥ #92 BULK/MOLE (BANDA DE TAMAÑO físico ETPL[type].size; rol = FUNCIÓN de IA arch — summoner menudo sz20⇒mole media 1 pero ENABLER rol 2; moose corpulento sz26⇒mole grande 2 pero BRUTE rol 0; DISJUNTOS). ⊥ #91 ZONE-TIER (dificultad del ÁREA donde muere [TERRENO]; rol = arquetipo del MOB [ENTIDAD], independiente de zona). ⊥ #86 SIEGA (e.hp/e.maxHp DINÁMICO; rol = categórico ESTÁTICO del template). ⊥ #85 CC (e.stun/e.slowT IMPUESTO; rol = identidad de spawn). ⊥ #84 ESCARAMUZA (CLASE DE ALCANCE e.tpl.ranged; rol = CAMPO arch DISTINTO, score NO rastrea ranged — enabler NO-ranged⇒2/#84 0, caster ranged⇒rol 0/#84 2 OPUESTOS, disruptor MEZCLA ranged warlock + melee volatile/punisher). ⊥ #76 VARIANTE (e.variant {stalker,bastion,glass}; rol lee ETPL[type].arch BASE — 'stalker' orco sigue arch brute⇒0). ⊥ #74 AFIJO (e.affix categórico; rol lee ETPL[type].arch BASE inmutable). ⊥ #73 APEX (DISTANCIA a jefe/campeón vivo; rol = arquetipo del mob sin distancia ni blanco). ⊥ #90 EMBESTIDA (DIRECCIÓN de movimiento), ⊥ #89 INTERRUPT (ESTADO DE ACCIÓN DINÁMICO e.state — un caster ocioso interrupt 0 vs casteando interrupt 2 con el MISMO arch; rol es identidad ESTÁTICA), ⊥ #88 REMATE (DISTANCIA hero↔víctima), ⊥ #87 MANADA (clustering mob↔mob), ⊥ #78 FURIA (BOOLEANO e.enraged), ⊥ #72 ESCASEZ (CONTEO crudo), ⊥ #69 LAST_STAND (CONTEO enganchados), ⊥ CADENCE #67/FRENZY/COMBO (tempo/DPS del HÉROE — el rol NO depende de cuántos golpes ni ritmo), ⊥ velocidad (summoner lento spd60 vs volatile veloz spd152 pero AMBOS con rol propio), ⊥ backstab/facing (ángulo), ⊥ nivel(inexistente)/edad/sigilo/terreno/clima/tiempo/social/territorial. ORTOGONAL a wardRegen/goldFind/oocMitigation/critChance/xpGain/vamp/lootQuality/restedMult/atkspd/detectRadius y toda la familia find (seams distintos).",
       gExists:(G.roleBounty!=null),                                      // prueba byte-id: STATELESS ⇒ G.roleBounty NUNCA se crea (0 estado nuevo, 0 clave serializada)
       hero:h?{ cls:h.cls, x:+(+h.x).toFixed(2), y:+(+h.y).toFixed(2), dead:!!h.dead, hp:+(+h.hp).toFixed(2), tx:Math.floor(h.x/TS), ty:Math.floor(h.y/TS), roleBounty:(h.roleBounty|0) }:null }; },
+  // CAS-2557: REMATE DE PRESA VALIOSA OBSERVABLE hook (DARK, WORTH_SURGE — eje VALOR/RECOMPENSA INTRÍNSECO del mob TYPE server-auth [worthWeight(víctima)=banda de ETPL[e.type].xp BASE via umbral hiXp/midXp: presa de ALTO valor (xp≥hiXp 50: demon/revenant/golem/dragon) ⇒ 2, presa de valor MEDIO (xp≥midXp 30: spearman/bandit/healer/orc/charger/moose) ⇒ 1, presa de bajo mérito (xp<midXp: rat/bat/wolf/volatile/skeleton) ⇒ 0] + canal FRESCO worthFind [marcas de recompensa por rematar a una presa de alto valor, sub-cap worthBountyCap]). Sólo lectura + drivers de PRUEBA gateados. Convergencia byte-a-byte: MISMO snapshot (tipos de mob replicados) ⇒ MISMO score/tier/marcas en N clientes.
+  //   worth()                                            → snapshot {enabled,channel,radius,weights,hiXp,midXp,tiers,cap,score,tier,charge,forageChargePreview,scoreProbe,xpProbe,spawnWorth,worthProbe,cleared,tag,gExists,hero}
+  //   worth({enabled})                                   → flip runtime IN-MEMORY de WORTH_SURGE.enabled (sin tocar el disco)
+  //   worth({tp:{tx,ty}})                                → MUEVE al héroe al CENTRO del tile (tx,ty)
+  //   worth({scoreProbe:{score}})                        → LUT PURA score→tier→charge (byte-verifica la TABLA + sub-cap, world/type-independiente)
+  //   worth({xpProbe:{xp}})                              → LUT PURA xp→weight→tier→charge (byte-verifica el path de BANDA hiXp/midXp, type-independiente)
+  //   worth({spawnWorth:{type,tx,ty}})                   → inyecta un mob de PRUEBA REAL del TIPO dado en el tile y lee worthWeight de su TIPO REAL (ETPL[type].xp BASE); devuelve idx + type + xp(base) + weight. overrideXp CLONA e.tpl.xp (mimetiza zona/afijo/campeón) ⇒ worthOf IGNORA el clon (lee BASE)
+  //   worth({clearWorth:true})                           → limpia los mobs de PRUEBA inyectados; devuelve nº removido
+  //   worth({worthProbe:true})                           → por cada mob VIVO en radio: {x,y,type,xp,weight} + score MAX (byte-verifica la lectura REAL server-auth del valor del tipo)
+  worth(p){
+    let scoreProbe=null, xpProbe=null, spawnWorth=null, worthProbe=null, cleared=null;
+    if(p && typeof p==="object"){
+      if("enabled" in p) WORTH_SURGE.enabled=!!p.enabled;
+      if(p.tp && G.hero){ G.hero.x=(p.tp.tx|0)*TS+TS/2; G.hero.y=(p.tp.ty|0)*TS+TS/2; }
+      if(p.scoreProbe && typeof p.scoreProbe==="object"){ const score=Math.max(0,+p.scoreProbe.score||0), t=worthRank(score);   // LUT PURA score→tier→charge (byte-verificación de la TABLA)
+        const raw=t>0?(+WORTH_SURGE.tiers[t-1].charge||0):0, cap=Math.max(0,WORTH_SURGE.worthBountyCap|0);
+        scoreProbe={ score, tier:t, charge:(cap>0?Math.min(cap,raw):raw)|0 }; }
+      if(p.xpProbe && typeof p.xpProbe==="object"){ const xp=Math.max(0,+p.xpProbe.xp||0), w=worthWeight({ type:"__probe__", tpl:{ xp } }), t=worthRank(w);   // LUT PURA xp→weight (banda hiXp/midXp) — usa el fallback e.tpl.xp al no existir ETPL["__probe__"]
+        const raw=t>0?(+WORTH_SURGE.tiers[t-1].charge||0):0, cap=Math.max(0,WORTH_SURGE.worthBountyCap|0);
+        xpProbe={ xp, weight:w, tier:t, charge:(cap>0?Math.min(cap,raw):raw)|0 }; }
+      if(p.spawnWorth && typeof p.spawnWorth==="object"){ const ty2=String(p.spawnWorth.type||"orc"), tx=p.spawnWorth.tx|0, tyy=p.spawnWorth.ty|0, sx=tx*TS+TS/2, sy=tyy*TS+TS/2;   // mob de PRUEBA REAL del tipo dado: lee worthWeight de su TIPO REAL (ETPL[type].xp BASE)
+        const e = ETPL[ty2] ? spawnEnemy(ty2, sx, sy) : null;
+        if(e){ e._worthTest=true; e.dead=false;
+          // ⊥#91/#74/⊥champion: SOBRESCRIBIR el CLON e.tpl.xp (mimetiza applyZoneScale/afijo/campeón que inflan e.tpl.xp); worthOf lee ETPL[type].xp BASE ⇒ IGNORA el clon sobrescrito. CLONA (no muta el ETPL compartido por referencia).
+          if("overrideXp" in p.spawnWorth){ e.tpl=Object.assign({},e.tpl,{ xp:+p.spawnWorth.overrideXp||0 }); e._worthOverridden=true; } }
+        spawnWorth={ idx:e?G.enemies.indexOf(e):-1, type:ty2, valid:!!ETPL[ty2], x:e?+e.x.toFixed(1):0, y:e?+e.y.toFixed(1):0,
+          xp:e?worthOf(e):0, tplXp:e?((e.tpl&&e.tpl.xp)||null):null, weight:e?worthWeight(e):0 }; }
+      if(p.clearWorth){ const before=(G.enemies||[]).length; G.enemies=(G.enemies||[]).filter(e=>!e._worthTest); cleared=before-G.enemies.length; }   // limpia SÓLO los mobs de prueba inyectados
+      if(p.worthProbe){ const h2=G.hero, R=+WORTH_SURGE.radius||0, R2=R*R, mobs=[]; let best=0;   // lectura REAL server-auth: mobs vivos en radio + su tipo/valor/peso
+        if(h2){ for(const e of (G.enemies||[])){ if(!e||e.dead||e.hp<=0) continue; const dx=h2.x-e.x, dy=h2.y-e.y, d2=dx*dx+dy*dy; if(d2>R2) continue;
+          const w=worthWeight(e); if(w>best) best=w;
+          mobs.push({ x:+e.x.toFixed(1), y:+e.y.toFixed(1), type:e.type, xp:worthOf(e), weight:w }); } }
+        worthProbe={ score:best, count:mobs.length, mobs }; }
+    }
+    const h=G.hero, vm=worthVM(h);
+    return { enabled:WORTH_SURGE.enabled, channel:WORTH_SURGE.channel||"worthFind",
+      radius:vm.radius, weights:Object.assign({},WORTH_SURGE.weights||{}), hiXp:(WORTH_SURGE.hiXp!=null?+WORTH_SURGE.hiXp:50), midXp:(WORTH_SURGE.midXp!=null?+WORTH_SURGE.midXp:30),
+      tiers:(WORTH_SURGE.tiers||[]).map(t=>({min:+t.min||0,charge:+t.charge||0})), cap:vm.cap,
+      score:vm.score, tier:vm.tier, tierCount:vm.tierCount, charge:vm.charge,
+      forageChargePreview: h?worthForage(h, {xp:100}):0,                 // preview: marcas forrajeadas por un remate con la mejor presa disponible (expone el canal worthFind; usa score EN VIVO)
+      scoreProbe: scoreProbe,                                            // LUT PURA (byte-verificación de la TABLA)
+      xpProbe: xpProbe,                                                  // LUT PURA xp→weight (byte-verifica la BANDA hiXp/midXp, type-independiente)
+      spawnWorth: spawnWorth,                                            // mob de PRUEBA inyectado (G.enemies) — idx + type + xp(base) + peso REAL server-auth
+      worthProbe: worthProbe,                                           // lectura REAL server-auth: mobs vivos en radio (score MAX + lista {x,y,type,xp,weight})
+      cleared: cleared,                                                 // nº de mobs de prueba removidos por clearWorth
+      tag: worthTag(h),                                                // glifo SERVIDO (OFF/presa de bajo mérito ⇒ "" / presa de valor≥medio ⇒ ✦)
+      precedence:"worthFind (canal FRESCO — recompensa de marcas de recompensa por REMATAR a una presa de ALTO VALOR/VALÍA — la presa codiciada del bestiario): NINGUNA de las 35 flags #59-#93 lo toca. La familia recompensa-de-forrajeo de moneda EXISTENTE (goldFind, lootQuality, xpGain, essenceFind, matFind, flaskPotency, gemFind, socketFind, healPotency, trophyFind #78, salvageFind #79, boneFind #80, frayFind #81, maelstromFind #82, blightFind #83, skirmishFind #84, controlFind #85, bloodFind #86, packFind #87, reachFind #88, interruptFind #89, headingFind #90, tierFind #91, bulkFind #92, roleFind #93) está LLENA ⇒ pivota a una moneda FRESCA, marcas de recompensa (h.worthBounty, recurso TRANSITORIO NUEVO, fuera del save allowlist + worldFingerprint). Fuente ÚNICA (seam de kill) ⇒ máximo-único trivial, sub-cap propio worthBountyCap, 0 doble-dip. PRE-FLIGHT GATE: el eje RECOMENDADO del issue (THREAT / DAÑO ACUMULADO que ESE mob infligió al héroe antes de morir) FALLA — NO existe un contador entero determinista por-mob del daño-infligido-al-héroe server-auth: damageHero (sim.js:8738) recibe `src`=el enemigo que pega pero lo usa SÓLO para enrutar parry/escudo/espíritu (src._sumAggro, h.parryT, SHIELD_BLOCK), NUNCA acumula src.dmgDealt/src.threat/e.dmgToHero; los únicos `dmgDealt` del código son LOCALES hero→enemy (probe guard-break sim.js:14748 hp0-e.hp) NO enemy→hero; spawnEnemy no estampa acumulador de daño-infligido. Añadir uno exigiría MUTAR el enemigo en el HOT-PATH de daño (damageHero corre por cada golpe recibido) con nuevo estado replicado per-tick ⇒ el issue manda NO forzarlo ⇒ pivote justificado al alterno FRESCO sancionado XP-WORTH. PASA: `xp` es un ESCALAR ENTERO ESTÁTICO por template (constante de ETPL, replicada del mismo config, NO wall-clock, NO estado de cliente, NO RNG, NO daño real infligido, NO DPS-del-héroe) y NINGUNA de las 35 flags previas lo lee como SCORE de kill (`.xp` sólo alimenta la CANTIDAD de XP en el chokepoint gainXP + reward de bounty-board + rested-xp — jamás una BANDA/peso/tier de kill). CLAVE ⊥#91/#74/⊥champion: worthWeight lee ETPL[e.type].xp (VALOR BASE INMUTABLE del TIPO), NO e.tpl.xp — applyZoneScale (sim.js:2303 xp:Math.round(b.xp*z.xpMul)), la promoción a campeón (2391 t.xp*=C.xpMul) y el afijo (2409 t.xp=b.xp*A.xpMul) TODOS inflan el CLON e.tpl.xp pero NUNCA la fila base de ETPL ⇒ valor DESACOPLADO de zona/afijo/campeón por construcción (desacople MÁS FUERTE que #92 size, que sólo afijo/campeón tocan; a xp lo tocan LOS TRES). EJE ESTÁTICO = worthWeight(e)=banda de worthOf(e): high(demon58/revenant64/golem220/dragon300, xp≥50) ⇒ 2, mid(spearman30/orc40/moose48, xp≥30) ⇒ 1, low(rat8/wolf12/skeleton20, xp<30) ⇒ 0. score del kill = worthWeight(víctima) muestreado en TOP (_worthPre); señal VIVA del badge = worthScore(hero)=MAX worthWeight sobre mobs VIVOS en radio. CRUX ⊥35: eje VALOR/RECOMPENSA INTRÍNSECO ESTÁTICO del TIPO, NO tamaño/rol/área/vida/conteo/geometría/estado/alcance/daño-infligido. ⊥ #92 BULK/MOLE (BANDA DE TAMAÑO físico ETPL[type].size; valor = MÉRITO de recompensa de diseño — INDEPENDIENTES en la tabla real: revenant size20 mole-media 1 pero xp64 valor ALTO 2; moose size26 mole-grande 2 pero xp48 valor MEDIO 1 ⇒ el mob MÁS PEQUEÑO vale MÁS ⇒ DISJUNTOS; skeleton size20/xp20 mole1/valor0 vs healer size19/xp32 mole1/valor1). ⊥ #93 ROL (arquetipo de IA arch categórico; valor = ESCALAR de recompensa — summoner arch enabler rol2/xp34 valor1 vs orc arch brute rol0/xp40 valor1 MISMO valor DISTINTO rol; demon warlock rol1/xp58 valor2 vs healer enabler rol2/xp32 valor1 valor OPUESTO al rol). ⊥ #91 ZONE-TIER (dificultad del ÁREA donde muere [TERRENO]; valor = mérito del MOB [ENTIDAD] BASE, y ADEMÁS worthOf ignora el xp zona-escalado del clon). ⊥ #86 SIEGA (e.hp/e.maxHp DINÁMICO; valor = escalar ESTÁTICO del template). ⊥ #85 CC (e.stun/e.slowT IMPUESTO; valor = identidad de spawn). ⊥ #84 ESCARAMUZA (CLASE DE ALCANCE e.tpl.ranged; valor = ESCALAR de xp, NO rastrea ranged — spearman ranged xp30 valor1 vs revenant melee xp64 valor2 OPUESTOS). ⊥ #76 VARIANTE (e.variant; valor lee ETPL[type].xp BASE). ⊥ #74 AFIJO (e.affix + e.tpl.xp inflado; valor lee ETPL[type].xp BASE inmutable). ⊥ #73 APEX (DISTANCIA a jefe/campeón vivo; valor = mérito del mob sin distancia). ⊥ #90 EMBESTIDA (DIRECCIÓN), ⊥ #89 INTERRUPT (ESTADO DE ACCIÓN DINÁMICO e.state — un mob ocioso vs casteando con el MISMO xp base tiene el MISMO valor; valor es identidad ESTÁTICA), ⊥ #88 REMATE (DISTANCIA hero↔víctima), ⊥ #87 MANADA (clustering mob↔mob), ⊥ #78 FURIA (BOOLEANO e.enraged), ⊥ #72 ESCASEZ (CONTEO crudo), ⊥ #69 LAST_STAND (CONTEO enganchados), ⊥ CADENCE #67/FRENZY/COMBO (tempo/DPS del HÉROE — el valor NO depende de cuántos golpes ni ritmo ni daño infligido), ⊥ THREAT (daño ACUMULADO por-mob al héroe — sin contador, y aunque existiera sería DINÁMICO run-time vs valor ESTÁTICO de tabla), ⊥ velocidad (volatile veloz spd152 xp16 valor0 vs summoner lento spd60 xp34 valor1), ⊥ backstab/facing, ⊥ nivel(inexistente)/edad/sigilo/terreno/clima/tiempo/social/territorial. ORTOGONAL a wardRegen/goldFind/oocMitigation/critChance/xpGain/vamp/lootQuality/restedMult/atkspd/detectRadius y toda la familia find (seams distintos).",
+      gExists:(G.worthBounty!=null),                                     // prueba byte-id: STATELESS ⇒ G.worthBounty NUNCA se crea (0 estado nuevo, 0 clave serializada)
+      hero:h?{ cls:h.cls, x:+(+h.x).toFixed(2), y:+(+h.y).toFixed(2), dead:!!h.dead, hp:+(+h.hp).toFixed(2), tx:Math.floor(h.x/TS), ty:Math.floor(h.y/TS), worthBounty:(h.worthBounty|0) }:null }; },
   // CAS-2380: DELVE / DESCENSO OBSERVABLE hook (DARK, DELVE — eje PROFUNDIDAD/DESCENSO VERTICAL + canal FRESCO critChance/precisión con CAP DURO). Sólo lectura + drivers de PRUEBA gateados (0 hotkey —
   // passive AMBIENTAL emerge del descenso, sin input.js). Convergencia byte-a-byte: MISMO snapshot+reloj ⇒ MISMO delve/bands/tier/crit en N clientes. INDIVIDUAL (per-pid, mirror trailcraft).
   //   delve()                                           → snapshot {enabled,channel,zones,tiers,...,self,zone,band,delve,bands,tier,critPct,critCapPct,critBonusPct,peer muls ⊥,tag,delveMap,bandsMap,gExists,nowMs,probe,critPicked,hero}
