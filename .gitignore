# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# Node / tooling
node_modules/
npm-debug.log*

# Python sprite tooling (tools-sprites/)
__pycache__/
*.pyc
.venv/
venv/

# Build / deploy artifacts
dist/
*.zip
deploy/

# Local dev server logs
*.log

# Headless QA screenshot artifacts (regenerable via npm run smoke / atlas-qa)
tools/*.png
shots/

# Raw purchased ERW Ancient Ruins tileset sheets (source art, ~5MB). The game ships
# only the tiny derived 32px tiles in assets/tiles|props/erw_*; regenerate them with
# tools/erw-slice-city.py after re-uploading the pack into assets/erw/city/.
assets/erw/

# Raw Tiled map source (14MB .tmx + tilesets) — the game ships the generated sim/tiled-map-data.js
assets/maps/src/
